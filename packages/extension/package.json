{
  "name": "@guardian/extension",
  "version": "0.1.0",
  "description": "Lumen Guardian MV3 extension — local-first child protection",
  "main": "dist/index.js",
  "scripts": {
    "build": "vite build",
    "zip": "npm run build && cd dist && zip -r ../guardian-extension.zip .",
    "clean": "rm -rf dist guardian-extension.zip",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [
    "browser-extension",
    "chrome",
    "firefox",
    "child-protection",
    "guardian"
  ],
  "author": "Lumen Guardian Team",
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.9.0",
    "@vitest/coverage-v8": "^2.1.8",
    "jsdom": "^25.0.1",
    "typescript": "^5.4.5",
    "vitest": "^2.1.8"
  }
}
